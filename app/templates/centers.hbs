<div class="template_header">
  <h1>Centers</h1>
</div>
<ul>
{{#each model as |center|}}
  <li><p>{{center.name}}</p>
  <ul>
    <li>{{center.address}}</li>
    <li>{{center.phone}}</li>
    {{#each center.class_ids as |class|}}
     <li>here{{class.id}}</li>
    {{/each}}
  </ul>
  </li>
{{/each}}
</ul>
{{outlet}}