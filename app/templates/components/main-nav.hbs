{{yield}}
{{menu-toggler}}
<ul class="togglable">
  {{#each navigation as |navElement|}}
  <li>{{#link-to navElement.route}}{{navElement.name}}{{/link-to}}</li>
  {{/each}}
  {{#each model as |navElement|}}
  <li>{{#link-to navElement.route}}{{navElement.name}}{{/link-to}}</li>
  {{/each}}
</ul>