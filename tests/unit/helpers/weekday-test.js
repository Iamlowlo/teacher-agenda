import { weekday } from '../../../helpers/weekday';
import { module, test } from 'qunit';

module('Unit | Helper | weekday');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weekday(42);
  assert.ok(result);
});
