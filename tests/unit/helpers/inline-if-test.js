import { inlineIf } from '../../../helpers/inline-if';
import { module, test } from 'qunit';

module('Unit | Helper | inline if');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inlineIf(42);
  assert.ok(result);
});
